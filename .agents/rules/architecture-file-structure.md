---
trigger: model_decision
---

"Follow the Feature-Based Pattern for the frontend. Place feature-specific components, hooks, and services inside src/features/[feature_name]. Shared UI components should stay in src/components. Never create a global .venv at the root; keep the virtual environment localized to the backend/ folder."